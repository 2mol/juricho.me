{
  "name": "juricho.me",
  "version": "1.0.1",
  "scripts": {
    "build": "NODE_ENV=production npx postcss -o static/tailwind.css styles/tailwind.css",
    "watch": "npx tailwindcss -i styles/styles.css -o static/styles/styles.css; zola serve"
  },
  "devDependencies": {
    "autoprefixer": "^10.2.6",
    "postcss": "^8.3.0",
    "postcss-cli": "^10.1.0",
    "tailwindcss": "^3.3.2"
  }
}
